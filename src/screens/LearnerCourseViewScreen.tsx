// Learner course view screen – uses existing CourseDetailScreen logic and APIs.
// This is just a naming wrapper so the learner flow can refer to it as
// \"Learner Course View\" while keeping the same backend contract (/courses/:id).
import CourseDetailScreen from './CourseDetailScreen';

export default CourseDetailScreen;

